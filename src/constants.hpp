#ifndef MENU_HPP
#define MENU_HPP

const int screenWidth = 1600;
const int screenHeight = 900;

const int buttonX = 100;
const int buttonWidth = 1400;
const int buttonHeight = 100;
const int fontSize = 50;

const int rows = 120; // Nombre de lignes
const int cols = 120; // Nombre de colonnes
const int cellSize = 40; // Taille de chaque cellule
#endif